[defaults]
inventory = inventory.yml
remote_user = flyk8s
host_key_checking = False
timeout = 30

[ssh_connection]
ssh_args = -o IdentitiesOnly=yes -o PreferredAuthentications=password -o PubkeyAuthentication=no

[privilege_escalation]
become = True
become_method = sudo
