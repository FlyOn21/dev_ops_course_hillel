[Unit]
Description=FastAPI Application
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/fastapi_app
ExecStart=/opt/fastapi_app/venv/bin/uvicorn main:app --host 0.0.0.0 --port {{ fastapi_port | default(8000) }}
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
