# TYPE  DATABASE        USER            ADDRESS                 METHOD
local   all             postgres                                peer
local   all             all                                     peer
# IPv4
host    all             all             127.0.0.1/32            scram-sha-256
# Allow FastAPI from vm_c network
host    {{ pg_db_name | default('fastapi_db') }}  {{ pg_db_user | default('fastapi_user') }}  192.168.102.0/24  scram-sha-256
# Allow from internal network
host    {{ pg_db_name | default('fastapi_db') }}  {{ pg_db_user | default('fastapi_user') }}  192.168.100.0/24  scram-sha-256
# IPv6
host    all             all             ::1/128                 scram-sha-256